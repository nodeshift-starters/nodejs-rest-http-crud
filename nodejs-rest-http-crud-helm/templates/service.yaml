apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejs-rest-http-crud-helm.fullname" . }}
  labels:
    {{- include "nodejs-rest-http-crud-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodejs-rest-http-crud-helm.selectorLabels" . | nindent 4 }}
